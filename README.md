# POC of svelte library styling

Using only CSS variables or SCSS, I'm trying to see how possible it is to make a component library that supports
- at least light and dark mode
- variants
- theme tokens
- override of styles
